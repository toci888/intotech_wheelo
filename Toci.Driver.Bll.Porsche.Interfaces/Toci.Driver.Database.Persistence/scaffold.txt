Install-Package Npgsql.EntityFrameworkCore.PostgreSQL -version 5
Install-Package Npgsql.EntityFrameworkCore.PostgreSQL.Design
Install-Package Microsoft.EntityFrameworkCore.Design -version 5

dotnet tool install --global dotnet-ef

dotnet ef dbcontext scaffold "Host=localhost;Database=Intotech.Wheelo;Username=postgres;Password=beatka" Npgsql.EntityFrameworkCore.PostgreSQL -o Models --project Toci.Driver.Database.Persistence -f

SELECT pid, usename, application_name, client_addr, query_start, state FROM pg_stat_activity WHERE datname = 'Intotech.Wheelo';
SELECT pg_terminate_backend(pid);
